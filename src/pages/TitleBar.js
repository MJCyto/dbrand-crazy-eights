import styled from "styled-components";

const Wrapper = styled.div`
  height: 30px;
  width: 100%;
  text-align: center;
`;

const TitleBar = () => {
  return <Wrapper>DBrand Crazy Eights</Wrapper>;
};

export default TitleBar;
